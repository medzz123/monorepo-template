{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "@trpc/server": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@tab-tab-y/api": "workspace:*",
    "@tab-tab-y/db": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "catalog:",
    "@types/cors": "^2.8.17",
    "tsx": "^4.19.2",
    "@types/node": "^22.13.11",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@tab-tab-y/config": "workspace:*"
  }
}
